# ============================================================
# Supabase — banco de dados e tempo real
# ------------------------------------------------------------
# Copie para ".env.local" e preencha com os dados do seu projeto.
# Painel do Supabase > Project Settings > API
#
# O MVP roda em modo DEMO (dados em memória) se estas variáveis
# estiverem em branco — útil para testar a interface sem backend.
# ------------------------------------------------------------

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
